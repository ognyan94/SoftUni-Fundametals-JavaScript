function nxNMatrix(num) {
  for (let i = 0; i < num; i++) {
    let numArr = [];
    for (let j = 0; j < num; j++) {
      numArr.push(num);
    }
    console.log(numArr.join(" "));
  }
}
nxNMatrix(3);
